# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Bubblewrap / Android build state and signing material
android/.gradle/
android/.bubblewrap/
android/app/build/
android/build/
android/local.properties
android/keystore/
android/*.apk
android/*.aab
android/*.idsig
android/assetlinks.json
android/service-account*.json
*.jks
*.keystore
key.properties
.env.android

# Capacitor / iOS build state and signing material
ios/App/Pods/
ios/App/build/
ios/DerivedData/
ios/*.ipa
ios/*.xcarchive
ios/*.mobileprovision
ios/signing-private/
*.p12
.env.ios

npm-debug.log*
yarn-debug.log*
yarn-error.log*
yarn.lock

*.pem

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/e2e/snapshots/

# playwright-cli
.playwright-cli/
test-*.png
# Artifacts (test output, logs, temp files)
/.artifacts/

# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream
